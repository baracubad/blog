<%= form_for [commentable, Comment.new] do |f| %>
  <%= f.text_area :body, placeholder: "Add a comment to #{commentable.class} ##{commentable.id}", class: "form-control mb-3" %>
  <%= f.submit "Submit", class: "btn btn-outline-primary btn-block" %>
<% end %>